$(function () {
    // Detect an Enter keypress to login
    // $box.on('keydown', 'input', function (event) {
    //     if(event.keyCode === '\r'.charCodeAt(0)) {
    //         $button.click();
    //     }
    // });
});
